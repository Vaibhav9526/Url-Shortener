services:
  postgres:
    image: postgres:17.4
    ports:
      - 5432:5432
    environment:
      POSTGRES_DB: postgres
      POSTGRES_USER: user
      POSTGRES_PASSWORD: pass

    volumes:
      - db_data:/var/lib/postgresql/data

volumes:
  db_data:
